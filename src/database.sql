-- CREATE DATABASE todo_app;
-- USE todo_app;

-- CREATE TABLE tasks (
--     taskId INT AUTO_INCREMENT,
--     taskText VARCHAR(70),
--     completed BOOLEAN,
--     PRIMARY KEY (taskId)
-- );

-- INSERT INTO tasks (taskText, completed) VALUES ("walk the dog", true);
-- INSERT INTO tasks (taskText, completed) VALUES ("buy cookies", false);
-- INSERT INTO tasks (taskText, completed) VALUES ("go to the gym", true);
